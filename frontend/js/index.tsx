import React, {StrictMode} from "react";
import ReactDOM from "react-dom/client";
import Root from "./Root";
import "../css/base.scss"

ReactDOM.createRoot(document.getElementById("root")!).render(<StrictMode><Root /></StrictMode>);